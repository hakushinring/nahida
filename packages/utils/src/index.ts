import './login'
import axios, { get, post, put, del, downPost, jsonp } from './request'
import { createContext, createModel } from './create-context'

export * from './create-context'

export { axios, get, post, put, del, downPost, jsonp }
export { createContext, createModel }
